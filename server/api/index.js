// Vercel serverless entry point.
// When deploying this folder to Vercel, every request is routed here by
// vercel.json and handled by the existing Express app (see ../app.js).
module.exports = require("../app");
