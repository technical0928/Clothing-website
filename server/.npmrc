# Skip dependency lifecycle scripts (e.g. express-rate-limit's "prepare" runs
# `husky` which is not installed and breaks `npm install` on Vercel/Render).
# Our own build script runs `prisma generate` explicitly, so nothing is lost.
ignore-scripts=true
